{
  "name": "nuxt-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "pm2dev": "pm2 start npm --name 'nuxt-project' -i 8 --watch -- run dev",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.11.0",
    "nuxt": "^2.13.0"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.2"
  }
}
